{
  "name": "sis-job",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute sis-job-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute sis-job-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "typescript": "^5.5.0",
    "@cloudflare/workers-types": "^4.20250101.0"
  }
}
